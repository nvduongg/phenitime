/** Root organization unit — single source of truth for seed & startup ensure. */
const ROOT_ORGANIZATION_UNIT = {
    unit_id: 'PKA',
    unit_name: 'Đại học Phenikaa',
    unit_type: 'UNIVERSITY',
    parent_id: null,
};

module.exports = {
    ROOT_ORGANIZATION_UNIT,
};
